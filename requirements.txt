# unifiles 依赖锁文件
# 生成时间: 2026-01-28
# 
# 安装方式:
#   pip install -r requirements.txt
#
# 或使用 pyproject.toml 安装:
#   pip install -e .          # 仅安装运行依赖
#   pip install -e ".[dev]"   # 安装运行依赖和开发依赖

# 核心依赖（运行必需）
pandas>=2.0.0
openpyxl>=3.1.0
pypdf>=3.0.0
python-docx>=1.0.0

# 开发依赖（可选，用于开发和测试）
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# mypy>=1.0.0
# pre-commit>=3.0.0
tomli>=2.0.0

# 当前安装的精确版本（用于锁定）
black==26.1.0
coverage==7.13.2
mypy==1.19.1
mypy-extensions==1.1.0
openpyxl==3.1.5
pandas==3.0.0
pypdf==6.6.2
pytest==9.0.2
pytest-cov==7.0.0
python-docx==1.2.0
